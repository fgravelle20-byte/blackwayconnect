{
  "name": "BlackWayConnect Engine",
  "install": "sudo apt-get update && sudo apt-get install -y --no-install-recommends python3.12-venv && python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements.txt",
  "terminals": [
    {
      "name": "api-server",
      "command": "source .venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8080 --reload",
      "description": "FastAPI backend served by uvicorn on port 8080 (health: /health, docs: /docs)."
    }
  ],
  "ports": [
    {
      "name": "api",
      "port": 8080
    }
  ]
}
